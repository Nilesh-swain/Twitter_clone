{
  "scripts": {
    "start": "cd Backend && npm run build && npm start"
  },
  "dependencies": {
    "nodemailer": "^7.0.6",
    "react-infinite-scroll-component": "^6.1.0"
  },
  "engines": {
    "node": "20.x"
  }
}
