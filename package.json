{
  "name": "twitter-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install && cd ../Frontend && rm -rf node_modules && NODE_ENV=production npm install && npm run build && mkdir -p ../Backend/dist && cp -r dist ../Backend/dist",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "nodemailer": "^7.0.6",
    "react-infinite-scroll-component": "^6.1.0"
  },
  "engines": {
    "node": "20.x"
  }
}
