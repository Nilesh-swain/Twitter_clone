{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "twitter-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install && cd ../Frontend && rmdir /s /q node_modules && del package-lock.json && set NODE_ENV=production && npm install && npm run build && mkdir ../Backend\\dist 2>nul || mkdir ../Backend\\dist && xcopy /e /i /y dist ../Backend\\dist",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "nodemailer": "^7.0.6",
    "react-infinite-scroll-component": "^6.1.0"
  },
  "engines": {
    "node": "20.x"
  }
}
