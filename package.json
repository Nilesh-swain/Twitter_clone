{
  "name": "twitter-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install && cd ../Frontend && if exist node_modules rd /s /q node_modules && if exist package-lock.json del package-lock.json && set NODE_ENV=production && npm install && npm run build && if not exist ../Backend/dist mkdir ../Backend\\dist && xcopy /e /i dist ../Backend\\dist",
    "start": "cd Backend && npm start"
  },
  "dependencies": {
    "nodemailer": "^7.0.6",
    "react-infinite-scroll-component": "^6.1.0"
  },
  "engines": {
    "node": "20.x"
  }
}
